--- a/src/zzuf.c
+++ b/src/zzuf.c
@@ -748,7 +748,7 @@
         char *tmpdir;
         tmpdir = getenv("TEMP");
         if (!tmpdir || !*tmpdir)
-            tmpdir = "/tmp";
+            tmpdir = "@MCHS_PREFIX@/tmp";
 
         int k = 0, extlen;
 
